name: Build & Deploy to MonsterASP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Medicares/src/Api/Medicares.Api/Medicares.Api.csproj

      - name: Build
        run: dotnet build Medicares/src/Api/Medicares.Api/Medicares.Api.csproj -c Release --no-restore

      - name: Publish
        run: dotnet publish Medicares/src/Api/Medicares.Api/Medicares.Api.csproj -c Release -o publish

      - name: Deploy to MonsterASP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: publish/
          server-dir: /wwwroot/
          dangerous-clean-slate: true
